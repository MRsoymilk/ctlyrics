[package]
name = "ctlyrics-tools"
version = "0.1.0"
edition = "2024"

[dependencies]
ctlyrics-utils = { path = "../utils" }
clap = { version = "4", features = ["derive"] }
regex = "1"
reqwest = { version = "0.12", features = ["blocking"] }
scraper = "0.20"
thiserror = "1"
urlencoding = "2"
